//
//  ContentView.swift
//  BaseConverter
//
//  Created by Jonathan Uriel Perez Vargas on 14/08/23.
//
// BaseShift: Numeric Base Converter
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
        .padding()
        .navigationBarTitle("Convert Units")
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
